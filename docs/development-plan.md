# Swanium Crystal 開発計画

## 目的と前提

Crystal で **SwanCrystal 固定**の WonderSwan 系エミュレータを実装する。Mono / WonderSwan Color ROM はSwanCrystalの後方互換として直接起動する。最初の対象は macOS 上のエミュレータ本体であり、自作・公開許可済みのテストプログラムと、利用者が合法的に用意した市販ゲームのローカル ROM を実行する。

- [ ] 最初の実装・配布・動作保証の対象を **macOS のみ** とする
- [ ] macOS で市販ゲームを安定して実行できる互換性を最優先し、Linux、Windows、その他のプラットフォームへの実装・CI・配布はその達成後に計画する
- [ ] 利用者が合法的に用意したローカル ROM の読み込みを実装対象とする。ただし、ROM の配布、暗号化回避、ダウンロード機能、ライブラリ管理は実装しない
- [ ] BIOS、商用ゲームデータなど権利者のあるデータをリポジトリに含めない
- [ ] コアを UI や OS API に依存させず、ヘッドレスでテスト可能にする
- [ ] 開発ツールのバージョン固定と実行を mise で統一する

## 参照実装との互換性契約

本プロジェクトは Rust 版 Swanium を参照実装とする Crystal 製品であり、エミュレーション機能は同等にする。実行機種はSwanCrystal固定、実 BIOS・BIOS 選択・BIOS 起動フローだけを明示的に対象外とする。それ以外のCPU、メモリマップ、割り込み、タイマ、DMA、PPU、APU、カートリッジ、RTC、保存データでは、各機能を「似たもの」として再実装するのではなく、元実装が受け入れている検証結果を移植版でも再現する。境界と個別の状態は[機能対応表](feature-compatibility.md)を正とする。

- [x] 元実装の `crates/core` を挙動の基準とし、対象機能と意図的な差分を[対応表](feature-compatibility.md)に記録する
- [ ] 実 BIOS と機種選択を除き、Rust 版のユニットテストおよび統合テストと同等のケースを Crystal の `spec/` に移す
- [ ] **WSCPUTest v0.7.1、ws-test-suite の採用済みケース、WSTimingTest v0.4.0（pages 0–28）、WSHWTest の `Test All` を全て通す**
- [ ] 公開テスト ROM はリポジトリに同梱せず、環境変数で与えたローカルパスだけをテストハーネスが読む
- [ ] タイルマップ、フレームバッファ、固定メモリ、終了条件を ROM ごとにソース確認して明文化し、推測による判定を行わない
- [ ] 追加の相違が出た場合は、命令トレース、フレーム状態、PCM ハッシュを比較して最初の差分サイクルを特定してから修正する

テスト対象と現時点の判定プロトコルは、元実装の `tests/README.md` と `crates/core/tests/public_roms.rs` を起点に Crystal 側へ移植する。移植途中も Rust 版のテスト総数・公開 ROM の通過ケース数を基準として差分を管理する。

## Crystal らしさを最優先する設計原則

互換性は必須だが、Rust の所有権モデル、trait、`Result`、スレッド設計を Crystal に逐語的に移さない。Crystal の標準ライブラリ、型システム、例外、fiber を生かし、Crystal 開発者が自然に読める・保守できる実装を成功条件の最上位に置く。

- [ ] **標準的な構成と命名**: `src/`、`spec/`、`bin/`、`lib/` の慣例に従う。名前空間とファイルパスを一致させ、型は `PascalCase`、メソッド・変数・ファイル名は `snake_case`、定数は `SCREAMING_SNAKE_CASE` にする。
- [ ] **型を使って状態を表す**: ハードウェア境界の整数は `UInt8` / `UInt16` / `UInt32` など幅を明示する。機種、割り込み、入力、レジスタ種別、実行状態は `enum` と値型を優先し、曖昧な整数フラグや文字列キーを避ける。
- [ ] **責務に合う型を選ぶ**: 小さく値として扱う状態は `struct`、ライフサイクル・可変状態を持つ装置は `class`、名前空間・共有実装は `module` を使う。Rust の trait に相当する複雑な抽象を模倣せず、本当に動的差し替えが必要な境界だけ `abstract class` または小さな module にする。
- [ ] **明快な API**: 失敗が通常の問い合わせ結果である場合は `foo?` と `Nil` を使う。設定不備、壊れたテストデータ、初期化失敗のような例外的な失敗は、意味のある独自 `Exception` を raise/rescue する。ホットパスで例外を制御フローに使わない。
- [ ] **決定論を壊さない並行処理**: コアは同期的な `run_cycles` / `run_frame` API とし、fiber・スレッド・時刻・グローバル可変状態を持ち込まない。フロントエンドで非同期処理が必要な場合だけ、エミュレーション状態の所有者を一つに固定し、`Channel` でコマンドやスナップショットを渡す。共有可変状態、fiber が同じ OS スレッドに戻るという仮定、実験的な並列実行には依存しない。
- [ ] **GC を意識した性能設計**: CPU、PPU、APU、DMA の内側のループでは一時配列、`String`、クロージャ、例外の生成を避ける。固定長バッファと再利用可能な値を使い、速度改善は必ずベンチマークと状態ハッシュで確認する。
- [ ] **安全な FFI**: SDL2 と将来必要な C API は `LibSDL` のような薄い `lib` 宣言に閉じ込める。`Pointer`、未初期化メモリ、ヌルポインタ、寿命管理は安全な Crystal ラッパーで検査してから上位へ公開する。
- [ ] **Crystal 標準の開発体験**: `crystal tool format` と `crystal spec` を常に CI で実行する。spec は振る舞いを短く表し、テーブル駆動の CPU テストには Crystal の配列・named tuple・共有 helper を用いる。メタプログラミングは重複を明確に減らせる場合だけに限定する。

レビューでは「元 Rust コードに似ているか」ではなく、(1) Crystal の慣例に沿うか、(2) 型と API が意図を表しているか、(3) GC と FFI を含めて性能・安全性が説明できるか、(4) 同等性テストを保っているか、の順で判断する。

### Fiber 活用方針

Fiber は UI の応答性を保つための手段として採用候補にするが、ハードウェアの時間モデルには使わない。

- [ ] 最初の SDL2 実装は fiber なしで始め、状態ハッシュと音声遅延を測定する
- [ ] 詰まりが計測された場合だけ、エミュレーション状態を一つの所有 fiber に移す
- [ ] SDL メイン fiber とエミュレーション所有 fiber の間を型付き `Channel` のコマンドで接続する
- [ ] CPU、PPU、APU、DMA、タイマを別々の fiber に分けない
- [ ] fiber 構成の導入前後で、状態ハッシュ、画像、PCM ハッシュの一致を回帰テストで確認する

## 方針変更: Slint を採用しない

Slint は UI 層として採用しない。エミュレータでは画面更新、入力遅延、音声、デバッグ表示を一体で扱う必要があり、UI フレームワークに実行ループを委ねる構成は切り分けが難しいためである。

代わりに、最初の実装では **SDL2 を薄いプラットフォーム層として使う**。

- [ ] ウィンドウ、描画先、ゲームパッド／キーボード入力、音声出力を SDL2 に集約する
- [ ] Crystal の SDL バインディングの機能不足や保守状況に依存しすぎないよう、必要最小限の C FFI をプロジェクト側に置く
- [ ] メニュー、設定、デバッガを初期段階ではキーボード操作とテキストオーバーレイで提供する
- [ ] よりリッチなネイティブ GUI が必要になった時点では、Crystal binding の [UIng](https://github.com/kojix2/uing) を第一候補として検証する

SDL2 は macOS で成熟しており、エミュレータに必要な責務を過不足なく満たす。将来ほかの OS や SDL3 を扱う場合も、`Platform` 抽象の内側に変更を閉じ込める。

### 将来のネイティブ GUI: UIng

初期リリースに UIng は依存として追加しない。必要な UI 要件が具体化してから、`frontend/ui` に閉じ込めた検証用実装で採否を決める。

- [ ] まず UIng が macOS で AppKit のネイティブ UI を使えることを検証する。Linux の GTK 検証は macOS 市販ゲーム互換性の達成後に行う
- [ ] UIng の対象を設定、入力割り当て、デバッグツール、ファイル／状態操作などの周辺 UI に限定する
- [ ] ゲーム画面のスケーリング、フレーム提示、ゲームパッド、音声は SDL2 を維持する
- [ ] UIng と SDL2 のイベントループを併用する場合は、まず macOS でウィンドウ所有者、メインスレッド要件、終了処理、入力フォーカス、fiber / Channel の境界を小さな実証アプリで確認する
- [ ] UIng のネイティブ資源の `destroy` / `free` を UI アダプタが一元管理する

## 想定アーキテクチャ

```text
app (CLI / SDL2 frontend)
        │
        ├── platform (window, input, audio, clock, filesystem)
        ├── debug    (overlay, trace, state dump)
        │
        └── emulator_core
              ├── cpu         (V30)
              ├── bus         (memory map, cartridge abstraction)
              ├── video       (tile / sprite / LCD timing)
              ├── audio       (sound channels / samples)
              ├── io          (buttons, RTC, interrupts, DMA)
              └── state       (save state, deterministic snapshots)
```

`emulator_core` は Crystal 標準ライブラリ以外に依存しない。フロントエンドはコアを一定クロックまたは一定フレーム数だけ進め、フレームバッファ、PCM、入力状態を受け渡すだけにする。

実行対象は `ProgramSource` 抽象を介して投入する。製品アプリは自作・公開許可済みプログラムに加え、利用者が合法的に用意したローカル ROM を明示的なファイル指定で受け付ける。ROM の収集・ダウンロード・カタログ化は行わない。テストハーネスは公開テスト ROM および個別に管理する市販ゲームのローカルパスを受け取り、元実装または事前に記録した検証条件と同じオラクルで実行する。

## ディレクトリ案

```text
src/
  swanium.cr
  swanium/
    core/          # プラットフォーム非依存のエミュレータ
    platform/      # SDL2 と OS ごとの差分
    frontend/      # 実行ループ、オーバーレイ、CLI
    test_programs/ # 自作・公開許可済みの小さな検証用プログラム
spec/
  core/
  integration/
tools/
docs/
  development-plan.md
  architecture.md
  compatibility.md
  testing.md
mise.toml
shard.yml
```

## CI と mise の方針

ローカル開発と CI の検証コマンドを乖離させないため、CI も mise を使う。GitHub Actions の YAML は OS ごとのシステム依存を導入してから `mise run ci` を呼ぶだけにし、ビルド・整形・テストの手順を YAML に重複して書かない。

- [x] `mise.toml` に Crystal と開発ツールの固定バージョン、`format`、`spec`、`build`、`ci` タスクを定義する
- [x] `ci` タスクを `format`、`spec`、`build` の集約タスクにし、ローカルと GitHub Actions の両方で `mise run ci` を実行する
- [x] GitHub Actions では `jdx/mise-action` を固定したバージョンまたはコミットで使用し、mise のツールキャッシュを有効にする
- [x] macOS job では Homebrew を用いて SDL2 と必要な開発ヘッダを明示的に導入する。OS パッケージは mise の管理対象にしない
- [x] 自作 fixture を使う通常 CI と、ローカル環境変数で公開テスト ROM を与えた opt-in 検証を分離する。通常 CI は `mise run ci`、公開テスト ROM のローカル検証は `mise run public-roms` を使う
- [x] `mise.lock` を導入した場合はリポジトリにコミットし、CI で解決済みバージョンを再現する

Crystal の公式 GitHub Actions ガイドは Crystal の導入方法の参考にするが、プロジェクト固有の検証入口は mise に統一する。

## 段階的な開発計画

### 0. 土台づくり

- [x] `mise.toml` に Crystal、LLVM、SDL2、フォーマッタ／静的解析ツールのバージョンを記録する。
- [x] `shard.yml`、`src/`、`spec/`、CI の最小構成を作る。
- [x] macOS で `mise install`、ビルド、ユニットテストを一つのコマンドで実行できる状態にする。
- [x] コーディング規約、エラー方針、ログ形式、ライセンス方針を `docs/` に記録する。

完了条件: 新しい macOS 環境で、mise だけを前提に空のアプリとテストを再現可能にビルドできる。

### 1. コアの最小実行系

- [x] V30 のレジスタ、命令デコーダ、フラグ、メモリアクセスを実装する。
- [x] 元実装の命令単位テストを Crystal の spec に 1 対 1 で移す。
- [x] バス、メモリマップ、割り込みコントローラ、タイマを実装する。
- [x] 1 命令ごとのトレースと、再現可能なスナップショットを用意する。

完了条件: ヘッドレスの自作テストプログラムが期待するレジスタ値、メモリ値、割り込み順序を示し、WSCPUTest の全項目を通過する。

検証済み: 自作の `STI → HLT → HBlank IRQ → ACK → IRET → HLT` プログラムを core spec で検証し、Rust 側 CPU 実テスト 123 件を Crystal spec に 1 対 1 で対応づけた。ローカル公開 ROM を `WS_CPU_TEST_ROM` で指定した WSCpuTest v0.7.1 は 58 フレームで `WSCPUTest passed` を報告する。

### 2. 映像と入力

- [x] LCD タイミング、背景、タイル、スプライト、パレットを段階的に実装する。
- [x] まず単色 WonderSwan、続いて Color / Crystal の拡張を追加する。
- [x] コアから RGBA フレームバッファを取得する API を固定する。
- [x] SDL2 フロントエンドで画面表示、整数倍率、キーボード／ゲームパッド入力を実装する。
- [x] フレームごとの画像比較テストを用意する。

完了条件: macOS 上で自作の画面・入力検証プログラムを 60 fps 相当で表示でき、期待画像との差分を CI で検出できる。

検証済み: 自作のColor / Crystal用タイル・スプライト画面を `mise run video-demo` で固定3倍表示し、SDL2の提示を60 Hzに制限した。ヘッドレス描画は120フレーム平均6.136 msで60 Hzの16.67 ms予算内だった。キーボードとSDL2ゲームパッドを同じ入力ビット列へ変換し、方向入力による画像変化をspecで確認した。CIでは224×144 RGBA画像（129,024バイト）を圧縮済み期待画像と完全比較する。

### 3. 音声、保存、デバッグ機能

- [x] 音源、音声タイミング、SDL2 のオーディオキューを実装する。
- [x] フレーム同期と音声バッファを調整し、音切れ・入力遅延を測定する。
- [x] セーブステートをバージョン付きの自前形式で実装し、保存先をテスト・設定データに限定する。
- [x] 一時停止、単一命令実行、メモリ表示、CPU トレース、画面レイヤー表示をオーバーレイで提供する。

完了条件: ヘッドレスと GUI 実行で同じ状態遷移を再現し、音声テストの波形ハッシュが安定する。

検証済み: 3.072 MHzの機械クロックから24 kHz・16 bitステレオPCMを生成し、4波形チャンネル、チャンネル2 PCM、チャンネル3スイープ、チャンネル4ノイズ、Color / CrystalのHyperVoiceを実装した。SDL2キューは約5フレームを上限にし、オーバーレイでキュー遅延とアンダーラン回数を確認できる。無音時のAPU処理は120フレーム平均1.371 msで、16.67 msのフレーム予算内だった。自作波形のPCM SHA-256、命令駆動と直接駆動の一致、全状態の保存・復元後のバイト完全一致をCIで検証する。RTCは実時間を参照せず、エミュレートクロックで進行し、セーブステートへ完全保存する。保存形式は`SWCST001`・version 3で、保存先はSwanium Crystal専用設定ディレクトリの0番スロットに限定した。

### 4. macOS 市販ゲーム互換性

- [x] **macOS**: SDL2 の導入手順、署名なし開発ビルド、Apple Silicon / Intel の確認を完了する。
- [x] 利用者が明示的に指定したローカル ROM を起動できるようにし、形式不正・未対応カートリッジ・保存データのエラーを安全に報告する。`.ws` / `.wsc` の明示指定、フッタ検査、SRAM／カートリッジEEPROM保存エラー報告まで実装済み。
- [x] 市販ゲームの検証対象を、利用者がローカルに保持するタイトルとそのバージョンに固定して管理する。ROM 本体、ハッシュ、スクリーンショット、セーブデータはリポジトリに含めない。
- [x] 採用済みタイトルごとに、起動、入力、映像、音声、ゲーム内セーブと復元、10分以上の連続実行を macOS 実機で確認し、`docs/compatibility.md` に記録する。
- [x] 確認済みの互換性差分は、命令トレース、フレーム状態、PCM ハッシュを用いて修正し、自作 fixture の回帰テストへ還元する。公開テスト ROM はローカル opt-in 検証として分離する。
- [x] macOS のヘッドレス統合テスト、画面スナップショットテスト、SDL2 起動確認を CI で実行する。

#### 段階4の残作業

**完了した実機確認**: Clock Tower、Makaimura、Final Fantasy 4、Last Alive、Wizardry、Wizardry: Scenario 1 を Apple Silicon と Intel の両方で確認し、操作、映像、音声、ゲーム内セーブ・復元、10分以上の連続実行を完了した。結果は `docs/compatibility.md` に記録する。

**完了した自動化**: CIは、市販ROMを使わない自作カートリッジを生成してCLIを900フレーム実行し、画面fixtureをspecで完全比較し、SDL2のdummyビデオドライバ初期化も確認する。これらはApple SiliconとIntelのmacOS runnerの両方で実行する。

**現在地**: Wizardry: Scenario 1 の負のチャンネル3スイープ値による算術オーバーフローは修正済みで、APUの回帰specへ追加済み。段階4の実機確認とCI自動化は完了した。公開テストROMを追加で採用する場合だけは、ローカルの `mise run public-roms` で合格を確認する（通常CIへROMは含めない）。

完了条件: Apple Silicon と Intel の macOS で、採用済み市販ゲームが起動・操作・映像・音声・保存を含めて安定して動作し、市販ROMを含めないCIの自作fixture検証を通過する。公開テストROMは採用した場合だけローカルのopt-in検証で通過させる。互換性の判定基準と既知の制限は `docs/compatibility.md` に記録する。

### 5. 他プラットフォーム展開（macOS 互換性達成後）

- [ ] macOS の完了条件を満たし、互換性修正が安定してから Linux の対応範囲、検証環境、配布方法を決める。
- [ ] Linux を着手する場合は Ubuntu LTS を基準に、Wayland / X11 の起動・入力・音声と macOS と同一の回帰テストを確認する。
- [ ] Windows は Crystal が Windows を正式サポートしてから別途計画する。それまでは `x86_64-windows-msvc`、MinGW-w64、MSYS2、Windows 専用 CI、DLL 同梱を試行しない。

完了条件: 各プラットフォームは macOS で確立した互換性テスト、状態ハッシュ、手動確認項目を満たしてから配布対象に加える。

### 6. 品質と公開準備

- [ ] パフォーマンス測定を自動化し、標準設定で実時間より十分速く動作することを確認する。
- [ ] 状態破損、異常入力、未対応機能を安全に報告する。
- [ ] 対応状況を `docs/compatibility.md` に公開する。ハードウェア機能に加え、検証済み市販ゲームはタイトル、検証した版、確認日、既知の制限を記録するが、ROM・ハッシュ・セーブデータは公開しない。
- [ ] ビルド手順、利用可能なテストプログラム、制限事項、ライセンスを README にまとめる。

## テスト戦略

- [ ] **ユニットテスト**: CPU 命令、フラグ、メモリマップ、タイマ、割り込み、描画プリミティブを検証する。
- [ ] **統合テスト**: 自作プログラムを決めたサイクル数実行し、CPU 状態・RAM・VRAM・音声ハッシュを照合する。
- [ ] **移植同等性テスト**: Rust 版と同じユニット／統合ケースを実行し、公開 ROM は元実装と同じ入力、起動方法、合否オラクルで照合する。
- [ ] **リグレッションテスト**: PNG フレームと PCM ハッシュを保存し、意図的に更新する場合だけレビューする。
- [ ] **差分調査**: 命令トレースとフレーム単位スナップショットを出力して、最初に異なるサイクルを特定する。
- [ ] **手動確認**: 実 OS ごとのウィンドウ、ゲームパッド、音声デバイス切替、サスペンド復帰を確認する。

テスト用データはすべて自作、または再配布が明確に許可されたものに限定する。

## 主要なリスクと対策

| リスク | 対策 |
| --- | --- |
| Crystal の FFI 差異 | SDL 呼び出しを `platform/sdl` に隔離し、少数の API だけを手書きバインドする。 |
| 描画タイミングの不正確さ | 描画を段階実装し、フレーム比較とサイクル単位トレースで検証する。 |
| OS ごとの入力・音声差 | まず macOS で API と互換性を固め、他プラットフォームは同じ受け渡し API と回帰テストに適合させる。 |
| 市販データの権利問題 | 市販 ROM・ハッシュ・セーブデータをリポジトリや CI に置かず、利用者がローカルで保持するデータだけを明示指定で扱う。 |
| UI 機能の肥大化 | まず SDL2 とオーバーレイに限定し、設定 UI は要件が固まってから追加する。 |

## 次に着手する内容

次の実装タスクは「段階 4」の macOS 市販ゲーム互換性である。まずローカル ROM 起動と macOS 実機の検証項目を固定し、採用済みタイトルの差分調査と回帰テスト化を進める。他プラットフォーム展開は、この完了条件を満たしてから着手する。
